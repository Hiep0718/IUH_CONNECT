You are an expert Software Architect and Full-Stack Developer. I am building a project called "IUH Connect" based strictly on a Microservices + Event-Driven architecture.

Tech Stack to strictly follow:
- Frontend: React Native (v0.73)
- Backend: Java Spring Boot (v3.2.0), Maven
- Message Broker: Apache Kafka 3.6 & Zookeeper
- Polyglot Databases: MariaDB, Redis 7.2, MongoDB 7.0
- Infrastructure: Docker & Docker Compose

I need to set up a clean, bug-free foundational project structure from scratch to push to Git. Do NOT write any business logic or endpoints yet. I only need the barebones folder structure and base configuration files.

For STEP 1, please generate:
1. A comprehensive root directory tree showing both `frontend/` (React Native) and `backend/` (Polyrepo setup for microservices). Include a root `.gitignore` suitable for a full-stack Java/React project.
2. A complete `docker-compose.yml` file in the root directory that spins up the ENTIRE infrastructure exactly as specified (Kafka, Zookeeper, MySQL, MongoDB, Redis). 
3. The skeleton folder tree for our first service: `backend/auth-service/` (include empty folders for `config/`, `controller/`, `dto/`, `model/`, `repository/`, `security/`, `service/`).
4. The base `pom.xml` for `auth-service` including dependencies for Spring Web, Spring Data JPA, MySQL Driver, Spring Security, and JWT.
5. The exact CLI command to initialize the React Native bare project inside the `frontend/` folder.

Wait for my explicit confirmation before moving to Step 2.



Awesome, the foundational structure from Step 1 looks great. Now, let's move to STEP 2: Implementing the Core Authentication Flow in `auth-service`.

Based on the architecture, the `auth-service` uses MariaDB for ACID compliance, BCrypt for password hashing, and JWT for stateless authentication.

Please generate the specific Java code for the following files inside `backend/auth-service/`:

1. `pom.xml` updates: Ensure you include the `mariadb-java-client` dependency instead of MySQL.
2. `src/main/resources/application.yml`: Configuration to connect to the MariaDB database defined in our Docker compose (use standard environment variables like spring.datasource.url=jdbc:mariadb://...).
3. `model/User.java`: JPA Entity mapped to a `users` table. Include fields: `id`, `username`, `password_hash`, and `avatar_url`.
4. `repository/UserRepository.java`: Spring Data JPA interface.
5. `security/JwtTokenProvider.java`: A utility class to generate, parse, and validate JWT access and refresh tokens.
6. `config/SecurityConfig.java`: Spring Security 6.x configuration (stateless session, disable CSRF, permit all requests to `/api/v1/auth/**`).
7. `controller/AuthController.java`: REST controller implementing the `POST /api/v1/auth/login` endpoint that accepts `{username, password}` and returns `{accessToken, refreshToken}`.

Please provide the code file-by-file. Let me know when you are ready for Step 3.




Prompt cho STEP 3: API Gateway & Đồng bộ dữ liệu User qua Kafka

"Chúng ta tiếp tục với STEP 3: Hoàn thiện Registration, Event-Driven cho User Profile và thiết lập API Gateway.

Trong auth-service (AuthController.java), thêm endpoint POST /api/v1/auth/register. Nó hash password, lưu vào MariaDB. Quan trọng: Ngay sau khi lưu thành công, hãy publish một event chứa {userId, username, avatarUrl} vào Kafka topic tên là user-events. Cung cấp code update cho AuthController và Kafka Producer Config tương ứng.

Khởi tạo Spring Boot project cho api-gateway/. Cung cấp file pom.xml (Spring Cloud Gateway, Actuator).

Cung cấp file application.yml cho Gateway. Cấu hình routing:

HTTP Requests bắt đầu bằng /api/v1/auth/** trỏ tới container auth-service.

WebSocket Requests bắt đầu bằng /ws/chat/** trỏ tới container chat-service (sử dụng giao thức ws://).

Hãy cung cấp code chi tiết từng file. Chờ xác nhận của tôi trước khi qua Step 4."

Prompt cho STEP 4: Chat Service - WebSocket Security & Consumer Đồng bộ User

"Rất tốt. Chuyển sang STEP 4: Xây dựng nền tảng cho chat-service với WebSocket Security và CQRS (Đồng bộ User).

Trong backend/chat-service/, hãy sinh code cho:

pom.xml: Spring WebSocket, Spring Kafka, Spring Data MongoDB, và thư viện JWT (để validate token).

model/ChatUser.java & repository/ChatUserRepository.java: Collection MongoDB lưu bản sao thông tin user (id, username, avatar_url) để read-optimize.

consumer/UserEventConsumer.java: Lắng nghe topic user-events từ Kafka, parse payload và lưu/cập nhật vào MongoDB thông qua ChatUserRepository.

security/JwtHandshakeInterceptor.java: Interceptor chặn kết nối WebSocket. Nó phải lấy JWT từ query parameter ?token=..., validate chữ ký (dùng chung secret key với auth-service), và từ chối kết nối nếu token sai.

config/WebSocketConfig.java: Đăng ký endpoint /ws/chat, add JwtHandshakeInterceptor và set AllowedOrigins là *.

Cung cấp code chi tiết file-by-file. Chờ xác nhận của tôi trước khi qua Step 5."

Prompt cho STEP 5: Trái tim của Chat Service - Xử lý tin nhắn với Kafka & Redis Pub/Sub

"Đã xong Step 4. Bây giờ là STEP 5: Xử lý luồng tin nhắn (Message Flow) giải quyết bài toán Scale Multi-node bằng Redis Pub/Sub và Kafka.

Trong chat-service/, hãy cung cấp code cho:

Cập nhật pom.xml thêm dependency cho Spring Data Redis.

model/MessageEntity.java & repository/MessageRepository.java (MongoDB): Lưu log tin nhắn. Đánh index trên conversationId và timestamp.

handler/ChatWebSocketHandler.java: Khi nhận JSON payload từ client, đóng gói thành ChatMessageDto và chỉ produce vào Kafka topic chat-messages (dùng conversationId làm routing key).

consumer/ChatMessageKafkaConsumer.java: Lắng nghe topic chat-messages, lưu message vào MongoDB (MessageRepository), sau đó publish chính message này lên Redis Pub/Sub channel tên là chat-channel.

redis/RedisMessageSubscriber.java: Lắng nghe channel chat-channel của Redis. Khi nhận được tin nhắn, tìm trong danh sách WebSocket sessions của node hiện tại xem người nhận (receiver) có đang kết nối không. Nếu có, đẩy tin nhắn qua WebSocket.

Thiết kế này đảm bảo dù có 10 node chat-service, user vẫn nhận được tin nhắn. Hãy cung cấp code chi tiết!"

Prompt cho STEP 6: Frontend React Native Integration

"Tuyệt vời, backend đã hoàn thiện kiến trúc phân tán! Hãy chốt hạ bằng STEP 6: React Native Chat Screen.

Trong thư mục frontend/, hãy sinh code cho:

Lệnh cài đặt react-native-gifted-chat.

Component ChatScreen.tsx:

Lấy JWT Token từ storage (mock tạm một biến string).

Khởi tạo WebSocket kết nối đến API Gateway: ws://localhost:8080/ws/chat?token={JWT_TOKEN}.

Lắng nghe event onmessage để parse JSON và hiển thị lên UI thông qua GiftedChat.

Hàm onSend: Khi user gõ tin nhắn, gửi JSON payload {senderId, receiverId, content, conversationId} qua connection WebSocket.

Đảm bảo code clean, quản lý vòng đời (lifecycle) của WebSocket (đóng kết nối khi unmount component) chuẩn xác trong React Native."